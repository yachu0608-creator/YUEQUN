# 越群汽車修配廠網站｜專案交接

整理日期：2026-09-04。此文件描述本次原始碼快照，不代表所有項目皆已驗收。

## 新電腦如何啟動

1. 解壓縮原始專案 ZIP，進入 `yuequn-website` 資料夾。
2. 安裝 Node.js 22.12 或以上的相容版本（本專案 Vite 的最低要求為 Node 20.19 或 22.12）。
3. 安裝 pnpm 10：`npm install -g pnpm@10`。
4. 在專案資料夾執行：

```sh
pnpm install --frozen-lockfile
pnpm dev --host 127.0.0.1 --port 4175
```

開啟終端機顯示的網址，通常是 http://127.0.0.1:4175/。不要直接雙擊 index.html。

```sh
pnpm build
pnpm preview --host 127.0.0.1 --port 4175
```

`build` 會先進行 TypeScript 檢查，再產生 dist。依賴首次安裝需要網路；新電腦全新安裝尚未實測。請保留 pnpm-lock.yaml，勿因 package.json 使用 latest 而隨意更新套件。

## 檔案與修改範圍

- src/App.tsx：頁面元件、內容資料及輪播互動。
- src/styles.css：所有版型、斷點及裝飾定位。
- public/assets：圖片、圖示、字型等本地資產，整包保留。
- design-source/images：從 D:\CODEX_SPEC.md\image 備份的 14 張完整原圖，逐檔 SHA-256 比對通過。這是備份，不會自動替換網站用圖，也不會被 Vite 複製到 dist。
- package.json、pnpm-lock.yaml、tsconfig*.json、vite.config.ts、index.html：執行與建置設定。
- design-qa.md：早期檢查紀錄；不可視為本次全部修改的驗收證明。
- 此為前端單頁網站，使用同頁錨點；没有後端、資料庫或預約 API。
- 原始專案包不含 node_modules、dist、Git 歷史、帳號登入資料或本次對話。

## 設計來源與已確認規則

Figma：https://www.figma.com/design/5peOriZLVOeZOf8d14BIAh/Untitled

新帳號需另外取得 Figma 檔案權限。ZIP 不包含可編輯的 Figma 原檔。

| 區塊 | 桌機 node-id | 平板 node-id | 手機 node-id |
| --- | --- | --- | --- |
| 全頁 | 173-160 | 388-893 | 306-227 |
| Banner | 173-327 | 388-894 | 315-921 |
| 安心維修 | 213-359 | 391-1262 | 321-1269 |
| 技師團隊 | 213-1021 | 394-1355 | 321-1405 |
| 車主評論 | 242-614 | 415-1588 | 415-1274 |
| 聯絡我們 | 264-276 | 415-1619 | 348-1769 |
| 頁尾 | 264-468 | 415-1605 | 348-1796 |

另：服务內容 438-1907、服務平板卡片 388-974、手機卡片 321-1391、技師照片 456-2001、手機回頂按鈕 359-2327。部分舊 Frame 可能與後續更新不同，以使用者最新設計及明確確認為準。

- 保留現有單頁區塊順序、素材、同頁導航，不自行重設計或增加路由。
- Nav 在 1200px 及以下改漢堡選單；紅色 active 狀態只對應目前捲動到的區塊，不保留已點擊狀態。
- Banner 手機斷點為 450px；人物需在不同尺寸維持適當居中裁切。
- 服務選項在 576px 及以下可水平捲動，不擠壓文字；卡片切換同步選項。
- 上下頁按鈕 hover 變紅底白箭頭，不放大縮小；線條粗細一致。
- 安心卡片 hover 陰影為 0 0 8px 0 rgba(0,0,0,.1)。桌機紅三角靠左下，平板手機靠右上。
- 技師說明：「經驗豐富，技術到位，用心照顧您的愛車。」桌機及平板側卡不顯示年資專長，移到中間才顯示；手機版型不顯示上下頁按鈕（目前 CSS 600px）。
- 技師資料：王志詺 / Michael Wang / 35年經驗 / 引擎系統診斷／底盤維修與調校；王豐穎 / Richard Wang / 7年經驗 / 冷氣系統檢修／清洗與保養；陳文卿 / Wen-ching Chen / 20年經驗 / 電腦診斷維修／電機系統檢修。
- 評論已依使用者要求撤回 Google 資料，恢復原設計四張相同示意內容：「王先生 / 台中市」、原頭像、五星與保養維修文字。不要把示意資料描述成已驗證真實評論。

## 尚未完成／正式上線前必查

1. Banner 每 5 秒自動輪播尚未實作；目前是手動按鈕、鍵盤及觸控切換。完成時應考慮暫停與 reduced-motion。
2. Banner 圖2、圖3目前網站仍使用 Figma 圖片節點裁切輸出（1394×663、1073×663）。完整原圖已保存在 design-source/images/banner圖 2.png 與 banner圖 3.png；後續應以原圖重新核對裁切。不要重新套用先前下載後全黑的資產。
3. 390～450px 圖片/遮罩寬度及齒輪右下定位已修改；手機各寬度完整視覺 QA 尚未完成。
4. 服務卡片已支援 pointer 左右滑動／滑鼠拖曳；桌機正反向切換驗證過。手機實機、跨斷點、連續滑動、首尾循環與頁面垂直捲動需再測。窄版目前是切換顯示，非卡片全程跟手拖移。
5. LINE 預約未接正式網址：部分 CTA 只跳 #contact，聯絡區按鈕顯示「預約連結準備中」。正式網址需使用者提供，不可猜測。
6. 評論是設計示意；公開上線前應由使用者確認是否保留或換成有授權的真實內容。確認人物照片、服務照片、字型使用權。
7. 最後一次本機型別檢查及建置通過；不等於所有斷點視覺驗收。測試 360、390、420、450、451、576、768、1200、1440、1920px，檢查空白、溢出、圖片裁切、陰影、nav、輪播與 console。

## 部署取得公開網址

可先使用 Netlify 的手動部署，不需要先購買網域。

1. 執行 pnpm build，確認產生的 dist 內有 index.html 及 assets。
2. 用自己的帳號登入 https://app.netlify.com/drop 。
3. 上傳 dist 資料夾（不是原始專案資料夾）。平台會產生網站網址；請確認方案及帳務條件，不要自動同意付費方案。
4. 公開後檢查圖片、同頁錨點、Google 地圖及手機版。公開网址意味任何有連結的人都可能存取。
5. 更新時重新 pnpm build，再到同一個網站的 Deploys 上傳新 dist，不要每次建立不同網站。

GitHub 目標為私人儲存庫 https://github.com/yachu0608-creator/YUEQUN ，網站檔案直接位於 repository 根目錄。Netlify Base directory 留空、建置命令 pnpm build、發布目錄 dist，已有 netlify.toml。使用根網域部署；若放在子路徑，現有 /assets 絕對路徑需另處理。自有網域與 DNS 是另外的設定；截至本交接文件，尚未完成公開部署或變更 DNS。

官方參考：https://docs.netlify.com/start/quickstarts/netlify-drop-quickstart/ 、https://docs.netlify.com/deploy/create-deploys/

## 給下一位協作者的訊息

「請先讀 HANDOFF.md，安裝並啟動此 React/Vite 專案。先回報現況與待辦，不要重做版型；只改我指定的項目。每次修改需建置、測互動並核對桌機/平板/手機，未驗證項目請明確說明。Figma 權限不足就告知，不要憑空替代素材。」
