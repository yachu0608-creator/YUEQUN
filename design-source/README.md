# 原始圖片備份

images 內的 14 個檔案原樣複製自使用者指定的原圖資料夾（實際路徑 D:\CODEX_SPEC.md\image），2026-09-04 複製後 SHA-256 逐檔比對一致。

這些圖片與 public/assets 中的網站裁切輸出不同。日後重新裁切應優先使用這裡的原圖。請保留檔名，不要以裁切圖覆寫原圖。

這個目錄不會自動發布到網站 dist；但會隨私人 GitHub 儲存庫交付給有權限的協作者。沒有包含 Figma .fig 檔，仍需使用者另外匯出保管。
